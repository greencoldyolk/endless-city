# A1 Far-City Background · Round 2 Manifest

Full final prompt used for all 8 images:

```text
Create this as an exact 2304×768 pixel, 3:1 opaque panoramic image.

A vast city skyline dissolving into rain haze at dusk, cinematic matte
painting, atmospheric realism. Cold slate-blue and blue-violet dusk ambient light with muted violet-gray clouds, plus a broad pale peach-pink horizon afterglow when warm. Values should stay lifted in the foggy distance, low-to-mid overall, with sparse-to-generous dim amber window/lamp lights depending on variant and never a black, oppressive sky. The skyline sits low and
far: building silhouettes recede layer by layer into fog toward a faint
mountain ridge dissolving in haze, and the sky fills the upper 55-60% of
the frame — keep the skyline low, the city must not crowd the sky. The
far city stays pale, barely darker than the sky itself, fog reading as
bright luminous air, every distant layer lifted toward the sky's value
by atmospheric perspective; no single tower dominates. Clouds are shapes
with visible soft edges and open pale sky around them, thinning toward
the top of the frame — never a heavy unbroken cloud ceiling. Moderate
fog under a dim warm dusk. Only sparse scattered window lights: a few
lonely lit windows here and there across the whole city, most windows
dark. The city continues naturally past both left and right edges of
the frame, with only a slight extra veil of haze at the outermost
margins — never an empty wall of fog. The mood is quiet and melancholic
yet gentle, never oppressive. ABSOLUTELY NO lit billboard screens of any
kind, no readable text, letters, logos, signage, neon, no people, faces,
vehicles, roads, or foreground objects anywhere in the image.
```

- `a1-r2-1.png` — fresh pull 1 in generation order; built-in imagegen returned 2172×724; archived unmodified, no refinement or post-processing.
- `a1-r2-2.png` — fresh pull 2 in generation order; built-in imagegen returned 2172×724; archived unmodified, no refinement or post-processing.
- `a1-r2-3.png` — fresh pull 3 in generation order; built-in imagegen returned 2172×724; archived unmodified, no refinement or post-processing.
- `a1-r2-4.png` — fresh pull 4 in generation order; built-in imagegen returned 2172×724; archived unmodified, no refinement or post-processing.
- `a1-r2-5.png` — fresh pull 5 in generation order; built-in imagegen returned 2172×724; archived unmodified, no refinement or post-processing.
- `a1-r2-6.png` — fresh pull 6 in generation order; built-in imagegen returned 2172×724; archived unmodified, no refinement or post-processing.
- `a1-r2-7.png` — fresh pull 7 in generation order; built-in imagegen returned 2172×724; archived unmodified, no refinement or post-processing.
- `a1-r2-8.png` — fresh pull 8 in generation order; built-in imagegen returned 2172×724; archived unmodified, no refinement or post-processing.
